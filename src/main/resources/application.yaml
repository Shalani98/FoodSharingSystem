spring:
  application:
    name: foodsharing


  datasource:
    url: jdbc:mysql://localhost:3306/food_sharing?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: ""   # replace with your MySQL password if any
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update